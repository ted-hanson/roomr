<h1>All Users</h1>

<% if @users.blank? %>
<p>No users to display</p>

<% else %>
    <ul id="users">
    <% @users.each do |user| %>
    <li>
       <%= link_to String(user.first_name) + ' ' + String(user.last_name), :action =>'show', :id =>user.id  -%>
    </li>
    <% end %>
</ul>
<% end %>
